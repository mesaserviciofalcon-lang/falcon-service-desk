"use client";

import { useState } from "react";
import toast from "react-hot-toast";

const factores = ["Roles y Responsabilidades", "Capacitación y Competencia", "Estandarización", "Recursos", "Aseguramiento", "Factores Externos"];

export default function FormularioSac({ simulacro }: { simulacro: { id: number; finca: string; tipo: string; conclusion: string; razonIncumplimiento: string | null; controlVulnerado: string | null } }) {
  const [descripcionSituacion, setDescripcionSituacion] = useState(`Resultado del ${simulacro.tipo}: ${simulacro.conclusion}`);
  const [correccion, setCorreccion] = useState("");
  const [tipoAccion, setTipoAccion] = useState("Correctiva"); const [responsableProceso, setResponsableProceso] = useState(""); const [proceso, setProceso] = useState("Seguridad"); const [sistemaGestion, setSistemaGestion] = useState("Seguridad Física"); const [norma, setNorma] = useState(""); const [requisito, setRequisito] = useState("");
  const [correcciones, setCorrecciones] = useState([{ actividad: "", responsable: "", fecha: "" }]);
  const [analisisCausa, setAnalisisCausa] = useState(simulacro.razonIncumplimiento || "");
  const [factoresCausa, setFactoresCausa] = useState<string[]>([]);
  const [planAccion, setPlanAccion] = useState([{ actividad: "", responsable: "", fecha: "" }]);
  const [comentariosCierre, setComentariosCierre] = useState("");
  const [eficacia, setEficacia] = useState(false);
  const [fechaReprogramacion, setFechaReprogramacion] = useState("");
  const [guardando, setGuardando] = useState(false);
  async function guardar() { setGuardando(true); try { const respuesta = await fetch(`/api/simulacros/${simulacro.id}/sac`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ descripcionSituacion, correccion, correcciones, analisisCausa, factoresCausa, planAccion, comentariosCierre, eficacia, fechaReprogramacion, tipoAccion, responsableProceso, proceso, sistemaGestion, norma, requisito }) }); const data = await respuesta.json(); if (!respuesta.ok) throw new Error(data.error || "No fue posible guardar la SAC"); toast.success("SAC cerrada, reprogramación solicitada y correo enviado."); window.location.href = `/api/simulacros/${simulacro.id}/sac/pdf`; } catch (error: any) { toast.error(error.message || "No fue posible guardar la SAC"); } finally { setGuardando(false); } }
  const filas = (titulo: string, datos: typeof planAccion, cambiar: (valor: typeof planAccion) => void) => <div><p className="font-semibold">{titulo}</p>{datos.map((item, indice) => <div key={indice} className="mt-2 grid gap-2 md:grid-cols-3"><input placeholder="Actividad" value={item.actividad} onChange={(e) => cambiar(datos.map((fila, i) => i === indice ? { ...fila, actividad: e.target.value } : fila))} className="rounded border p-3" /><input placeholder="Responsable" value={item.responsable} onChange={(e) => cambiar(datos.map((fila, i) => i === indice ? { ...fila, responsable: e.target.value } : fila))} className="rounded border p-3" /><input type="date" value={item.fecha} onChange={(e) => cambiar(datos.map((fila, i) => i === indice ? { ...fila, fecha: e.target.value } : fila))} className="rounded border p-3" /></div>)}<button type="button" onClick={() => cambiar([...datos, { actividad: "", responsable: "", fecha: "" }])} className="mt-3 rounded border px-3 py-2 text-sm font-semibold">Agregar fila</button></div>;
  return <div className="mx-auto max-w-4xl space-y-5"><div><h1 className="text-3xl font-bold text-[#0F3D1F]">Solicitud de Acción</h1><p className="mt-1 text-slate-600">Originada en {simulacro.tipo} · {simulacro.finca}</p></div><section className="space-y-4 rounded-xl border bg-white p-5 shadow-sm"><div className="grid gap-3 md:grid-cols-3"><label>Tipo de acción<select value={tipoAccion} onChange={(e) => setTipoAccion(e.target.value)} className="mt-1 w-full rounded border p-3"><option>Correctiva</option><option>Preventiva</option><option>Mejora</option></select></label><label>Responsable<input value={responsableProceso} onChange={(e) => setResponsableProceso(e.target.value)} className="mt-1 w-full rounded border p-3" /></label><label>Proceso<input value={proceso} onChange={(e) => setProceso(e.target.value)} className="mt-1 w-full rounded border p-3" /></label><label>Sistema de gestión<select value={sistemaGestion} onChange={(e) => setSistemaGestion(e.target.value)} className="mt-1 w-full rounded border p-3"><option>Seguridad Física</option><option>Calidad</option><option>Ambiental</option><option>Social</option><option>SST</option></select></label><label>Norma<input value={norma} onChange={(e) => setNorma(e.target.value)} className="mt-1 w-full rounded border p-3" /></label><label>Requisito<input value={requisito} onChange={(e) => setRequisito(e.target.value)} className="mt-1 w-full rounded border p-3" /></label></div><label className="block font-semibold">1. Descripción de la situación (hallazgo + evidencia objetiva)<textarea value={descripcionSituacion} onChange={(e) => setDescripcionSituacion(e.target.value)} className="mt-1 min-h-28 w-full rounded border p-3" /></label>{filas("2. Corrección (si aplica)", correcciones, setCorrecciones)}<label className="block font-semibold">3. Análisis de causa raíz<textarea value={analisisCausa} onChange={(e) => setAnalisisCausa(e.target.value)} className="mt-1 min-h-28 w-full rounded border p-3" /></label><div><p className="font-semibold">Factores de causa</p>{factores.map((factor) => <label key={factor} className="mt-2 flex gap-2 text-sm"><input type="checkbox" checked={factoresCausa.includes(factor)} onChange={(e) => setFactoresCausa((actual) => e.target.checked ? [...actual, factor] : actual.filter((item) => item !== factor))} />{factor}</label>)}</div>{filas("4. Plan de acción frente a la causa raíz", planAccion, setPlanAccion)}<label className="block font-semibold">Fecha solicitada para reprogramar el simulacro<input required type="date" value={fechaReprogramacion} onChange={(e) => setFechaReprogramacion(e.target.value)} className="mt-1 w-full rounded border p-3" /></label><label className="block font-semibold">5. Seguimiento / comentarios de cierre<textarea value={comentariosCierre} onChange={(e) => setComentariosCierre(e.target.value)} className="mt-1 min-h-24 w-full rounded border p-3" /></label><label className="flex gap-2 font-semibold"><input type="checkbox" checked={eficacia} onChange={(e) => setEficacia(e.target.checked)} />6. Las acciones emprendidas fueron eficaces y la SAC se cierra</label><button disabled={guardando} onClick={guardar} className="rounded-lg bg-[#0F3D1F] px-5 py-3 font-semibold text-white disabled:bg-slate-400">{guardando ? "Guardando..." : "Cerrar SAC y generar PDF"}</button></section></div>;
}
